name: Aider AI Coder

on:
  issues:
    types: [opened, edited]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  aider:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Aider and required dependencies
        run: |
          python -m pip install --upgrade pip
          pip install aider-chat

      - name: Execute Aider to Fix Code
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Safely passes the issue text to prevent terminal command execution bugs
          ISSUE_BODY: ${{ github.event.issue.body }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@://github.com"
          
          # Added --stream flag for headless runs and properly encapsulated the prompt variable
          aider --model gemini/gemini-2.5-flash --yes --stream --message "$ISSUE_BODY"

      - name: Push updates back to your branch
        run: |
          git push origin HEAD:main || echo "No changes to push"
